<%- include('privateHeader') -%>

<h1>Bienvenue sur la page des créneaux réservés !</h1>

<div class="reservationsDisplay">
    <% if(interventions.length == 0) { %>

    <div class="alert alert-primary" role="alert">
        Vous n'avez aucune réservation à venir.
    </div>

    <% } else { %>

    <table class="table table-hover">
        <thead class="thead-dark">
            <th scope="col">Intervention(s) à venir : </th>
        </thead>
        <tbody>
            <% for (intervention of interventions) { %>

            <tr>
                <td class="table-active">
                    <!-- <form action="/user/reservations" method="POST"> -->
                    <%= cpt %> - <%= intervention %>
                    <!-- <%cpt ++ %>
                        <div class="form-check">
                            <input class="form-check-input position-static" type="radio" name="blankRadio"
                                id="blankRadio1" value="<%= intervention %>" aria-label="...">
                        </div> -->
                </td>
            </tr>
            <% } %>
        </tbody>
    </table>
    <!-- <button name="del" id="delButton" type="submit" class="btn btn-danger">Supprimer l'intervention sélectionnée</button>
    </form> -->
    <% } %>

</div>
<%- include('../footer') -%>